{
  "version": 1,
  "storiesDir": "stories",
  "merge": "self",
  "gates": {
    "check": { "kind": "command", "run": "true" }
  },
  "defaults": { "chore": ["check"] },
  "gateLock": true,
  "budgets": { "maxStalls": 2, "maxFixRoundsPerStory": 2 }
}
