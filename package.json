{
  "name": "claude-kit-tests",
  "private": true,
  "scripts": {
    "test": "bun test tests/static",
    "test:evals": "bun test tests/evals --concurrent --max-concurrency 8 --timeout 120000",
    "test:evals:smoke": "bun test tests/utils/config-dir.test.ts --timeout 60000",
    "test:all": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/mdast": "^4",
    "@types/unist": "^3",
    "yaml": "^2",
    "unified": "^11",
    "remark-parse": "^11"
  }
}
