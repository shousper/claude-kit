# Third-party notice

The rule files under `styles/Writing/` are copied from the `Google` Vale package (https://github.com/vale-cli/Google), MIT licensed, renamed to a neutral style name. The guidance they encode comes from the Google developer documentation style guide, CC BY 4.0. Copied 2026-09-05 with `vale sync`; refresh the same way and re-run `tests/static/vale-style.test.ts`.
